# Shared risk thresholds for gate scan + dashboard.

# Daily stop
daily_stop_pct: 4.0
daily_stop_usd: 40.0
daily_stop_equity: 1000.0
daily_stop_streak_window_days: 7
daily_stop_streak_count: 3
daily_stop_pause_days: 3
daily_stop_warn_window_days: 14
daily_stop_warn_count: 5
daily_stop_escalate_window_days: 21
daily_stop_escalate_count: 7

# Range-break circuit
range_break_symbol: BTC
range_break_days: 7
range_break_atr_mult: 0.5
range_break_confirmed_only: true

# Baseline gate defaults
baseline_max_open: 10
baseline_max_per_cluster: 3
baseline_atr_mult: 0.8
baseline_rr: 1.5
baseline_atr_mode: clipped
baseline_expiry: 30d
baseline_position_pct: 3.75
baseline_portfolio_usd: 5000.0
baseline_live_position_usd: 187.5
baseline_leverage: 50.0
